const parImpar = (numero) =>{
   // if (numero % 2 === 0){
     //   return true;

    //} else{
      //  return false;
    //}
    return numero % 2 === 0 ? true : false;
};

//las funciones anonimos si solamente voy a utilizar una linea y voy a retonar
const esPositivo = (numero) => (numero >= 0 ? true : false)